#!/usr/bin/env bash
set -euo pipefail

export PATH="/opt/homebrew/bin:/usr/local/bin:$HOME/.foundry/bin:$PATH"

echo "[pre-commit] lint"
pnpm -s lint

echo "[pre-commit] typecheck"
pnpm -s typecheck

echo "[pre-commit] api tests"
pnpm -s -C apps/api test
