# Copy to .env.local and fill as needed.

NEXT_PUBLIC_API_HTTP_URL=http://localhost:3001
NEXT_PUBLIC_API_WS_URL=ws://localhost:3001/ws

# ENS (Sepolia)
# Deploy `AgentArenaSubnameRegistrar` and set these for the web app.
NEXT_PUBLIC_ENS_REGISTRAR_ADDRESS=
# Namehash of your parent name (bytes32 as 0x...)
NEXT_PUBLIC_ENS_PARENT_NAMEHASH=
# Display-only parent name (e.g. agentarena.eth)
NEXT_PUBLIC_ENS_PARENT_NAME=agentarena.eth
